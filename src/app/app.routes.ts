import { Routes } from '@angular/router';
import { CheckListComponent } from './views/check-list/check-list.component';

export const routes: Routes = [
  {
    path: '',
    component: CheckListComponent,
  },
];
